{
  "name": "hellosite-demo-engine",
  "version": "2.0.0",
  "description": "HelloSite Demo Engine — generates live demo sites from Google Place IDs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "stripe": "^16.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
